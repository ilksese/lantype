{
  "identifier": "default",
  "description": "Default capability for LanType",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "shell:allow-open"
  ]
}